projection;
strict ( 2 );
use draft;
define behavior for ZVKS001_C_TRAVELM alias Travel
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}